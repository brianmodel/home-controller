[package]
name = "eye-anim"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Hardware-independent animation/easing math for the ESP32-S3 eyes (host-testable)"

[dependencies]

[dev-dependencies]
